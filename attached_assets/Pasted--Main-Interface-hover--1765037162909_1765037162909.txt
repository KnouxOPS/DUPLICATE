واجهة المستخدم: لا توجد تحسينات فعلية على Main Interface، لا توجد انتقالات سلسة، لا تأثيرات hover، لا الظلال أو Glow للأزرار، ولا أي micro-interactions.

شاشة البداية (Splash Screen): لا توجد أي حركة للشعار أو الخلفية المتحركة أو التأثيرات Fade/Zoom/Rotation.

الوضع الليلي / Light-Dark Mode: لم يتم تطبيق أي تغييرات.

صفحة الاتصال: لم تُنشأ، ولم تُدرج المعلومات الخاصة بالاتصال (Eng / Sadek elgazar، +971503281920، knouxops@gmail.com
).

مراجعة المشروع والتأكد من التكامل: لم يتم اختبار أي ميزة أو مراجعة المخرجات للتأكد من عمل كل شيء بسلاسة.

المطلوب فورًا وبشكل صارم:

1️⃣ مراجعة شاملة للملفات:

JS, TS, React Components, Assets.

التأكد من توافق كل dependencies، وملفات package.json وpnpm-lock.yaml، ووجود node_modules مكتمل.

التأكد أن كل خدمة أو مكون يعمل بشكل فردي ومتكامل مع المشروع.

2️⃣ إعداد المشروع والبناء:

إيقاف أي عمليات dev جارية.

تنظيف الملفات المؤقتة بدون حذف الملفات المصدرية.

تثبيت Node.js + npm + pnpm إن لم تكن موجودة.

تثبيت جميع dependencies عبر pnpm.

تشغيل dev mode لاختبار كل الوظائف.

بناء EXE باستخدام electron-builder بدون أي تدخل يدوي.

3️⃣ تحسينات UI/UX كاملة:

Splash Screen: حركة سلسة للشعار، fade-in + zoom أو rotation، مدة 2–4 ثوانٍ، خلفية gradient أو particle animation.

واجهة رئيسية Main Interface: ألوان متناسقة، خطوط واضحة، تأثيرات hover/Glow/Shadow، transitions سلسة، Optional: Dark/Light Mode toggle.

Micro-interactions: عند الضغط أو التمرير.

4️⃣ التأكد من عمل كل الميزات:

اختبار كل ميزة فردية وتكاملها مع باقي المشروع.

التأكد من عمل main & renderer processes، كل APIs، خدمات AI، والميديا.

EXE النهائي يجب أن يعمل بدون أي خطأ.

5️⃣ صفحة الاتصال Contact Page:

إضافة صفحة جديدة مع:

Eng / Sadek elgazar

Tel & WhatsApp: +971503281920

Email: knouxops@gmail.com

أيقونات لكل وسيلة، فتح WhatsApp مباشرة، وفتح البريد الافتراضي عند النقر.

تصميم متناسق مع الثيم الفاخر، مع subtle animations عند الضغط على الروابط.

6️⃣ الفحص النهائي والنشر:

التأكد من وجود EXE في مجلد dist.

اختبار Splash Screen، جميع الميزات، صفحة Contact.

عدم حذف أي ميزة حالية.

تنويه هام:

العمل على المشروع الحالي فقط، لا إنشاء مشروع جديد.

جميع التحسينات يجب تطبيقها فعليًا، ومراجعة المشروع بالكامل قبل تسليم EXE.

الهدف: EXE جاهز للتشغيل، واجهة فاخرة، تجربة مستخدم سلسة، كل شيء يعمل بشكل متكامل.